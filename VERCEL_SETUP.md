# Vercel 部署配置说明

## 🚀 部署到 Vercel 并配置 Edge Config

### 步骤 1：创建 Edge Config

1. 登录 [Vercel Dashboard](https://vercel.com/dashboard)
2. 选择您的项目
3. 点击顶部导航栏的 **Storage** 标签
4. 点击 **Create Database**
5. 选择 **Edge Config**
6. 输入名称：`jinqiu-content`
7. 点击 **Create**

### 步骤 2：初始化数据

创建 Edge Config 后，需要添加初始数据：

1. 在 Edge Config 页面，点击 **Items** 标签
2. 点击 **Add Item**
3. 添加第一个键值对：
   - Key: `content`
   - Value: 复制 `public/data/content.json` 的内容
4. 点击 **Add Item** 再添加第二个：
   - Key: `team`
   - Value: 复制 `public/data/team.json` 的内容
5. 点击 **Save**

### 步骤 3：配置环境变量

1. 在项目页面，点击 **Settings** 标签
2. 点击左侧的 **Environment Variables**
3. 添加以下环境变量：

#### 必需的环境变量：

```bash
# 管理员密码（请修改为强密码）
ADMIN_PASSWORD=your_secure_password_here

# Edge Config 连接字符串（Vercel 会自动添加，无需手动设置）
EDGE_CONFIG=（自动生成）

# Edge Config ID（在 Edge Config 设置页面可以找到）
EDGE_CONFIG_ID=ecfg_xxxxxxxxxxxxx

# Vercel API Token（用于更新 Edge Config）
VERCEL_API_TOKEN=（需要创建）
```

### 步骤 4：创建 Vercel API Token

1. 点击右上角头像 → **Settings**
2. 点击左侧 **Tokens**
3. 点击 **Create Token**
4. 输入名称：`Jinqiu Admin API`
5. Scope 选择：**Full Access** 或只选择您的项目
6. 点击 **Create**
7. **复制生成的 Token**（只显示一次！）
8. 回到项目的 Environment Variables，添加：
   - Name: `VERCEL_API_TOKEN`
   - Value: 粘贴刚才复制的 Token

### 步骤 5：重新部署

1. 点击项目页面的 **Deployments** 标签
2. 点击最新的部署右侧的 **...** → **Redeploy**
3. 确保 **Use existing Build Cache** 未勾选
4. 点击 **Redeploy**

---

## ✅ 验证配置

部署完成后：

1. 访问 `https://your-domain.com/admin`
2. 输入管理员密码登录
3. 修改一些内容并保存
4. 刷新前台页面，确认内容已更新
5. 检查 Edge Config Dashboard，确认数据已更新

---

## 🔧 本地开发

本地开发**不需要**配置 Edge Config，会自动使用 JSON 文件。

只需创建 `.env.local` 文件：

```bash
# .env.local
ADMIN_PASSWORD=jinqiu2025
```

然后运行：

```bash
npm run dev
# 或
pnpm dev
```

访问 `http://localhost:3000/admin` 即可使用管理后台。

---

## 📊 工作原理

### 本地环境
- ✅ 读取：从 `public/data/*.json` 文件读取
- ✅ 写入：保存到 `public/data/*.json` 文件
- ✅ 无需数据库

### 生产环境（Vercel）
- ✅ 读取：从 Edge Config 读取（<1ms 延迟）
- ✅ 写入：通过 Vercel API 更新 Edge Config
- ✅ 备份：同时也保存到 JSON 文件（但 Vercel 无法持久化）

---

## 🆘 故障排查

### 问题 1：保存后内容没有更新

**检查：**
1. 确认 `EDGE_CONFIG_ID` 和 `VERCEL_API_TOKEN` 已正确配置
2. 在 Vercel Dashboard 的 Edge Config 页面，检查数据是否已更新
3. 查看 Vercel 的 Function Logs，查找错误信息

### 问题 2：本地无法保存

**检查：**
1. 确认有文件写入权限
2. 确认 `public/data/` 目录存在
3. 查看浏览器控制台是否有错误

### 问题 3：Edge Config 更新失败

**可能原因：**
- `VERCEL_API_TOKEN` 权限不足
- `EDGE_CONFIG_ID` 不正确
- API Token 已过期

**解决方案：**
重新创建 API Token 并更新环境变量。

---

## 💰 费用说明

- **Edge Config**：Hobby 计划免费，每月包含：
  - 1 个 Edge Config
  - 无限次读取
  - 1,000 次写入
  
对于内容管理系统，这个额度完全够用！

---

## 🔄 数据迁移

如果需要从 JSON 文件迁移到 Edge Config：

1. 确保本地 `public/data/*.json` 有最新数据
2. 复制 `content.json` 和 `team.json` 的内容
3. 在 Vercel Edge Config Dashboard 更新对应的键值
4. 保存并重新部署

---

**配置完成后，您的管理后台就可以在生产环境正常工作了！** 🎉

